# Source for Images for Enterprise releases

This repo contains the source for the images used in the Enterprise releases.
Images can be tested on a Google Cloud machine with a remote CAS on Azure.

## Testing the images

Use the `run` script

```sh
./run.sh
```

More TBD
